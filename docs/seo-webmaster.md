# Vrompt search operations

The public, indexable product surfaces are the homepage, pricing, features,
models, Auto, help, privacy and terms pages. Authenticated workspace routes,
conversation URLs, files, usage and administration must remain `noindex`.

Do not publish provider credentials, internal cost data, conversation content or
model routing policy in metadata. Review redirects after retiring old prompt
URLs; redirect only when the destination represents the same user intent.
